# GameDevelopmentGroupProject

### How to commit and push changes

Make sure all commands are done within the root of the project file in your command line.

#### To pull changes
- 'git pull origin main' 

#### To push changes
- 'git add .'
- 'git commit -m "<message>"
- 'git push origin main'
